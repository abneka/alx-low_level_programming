#include <stdio.h>

/**
 *  main - task no 02
 *
 *  Return: Always 0 (Success)
 */

int main(void)
{
	char low;

	char high;

	for (low = 'a'; low <= 'z'; low++)
	{
		putchar(low);
		putchar(high);
	}
	putchar('\n');
	return (0);
}
